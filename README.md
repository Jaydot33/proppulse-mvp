# proppulse-mvp
Real-time NBA prop bets with X beat writer intel &amp; ML adjustments (or shorter: MVP: PropPulse - Live props + Twitter edge)

## Local Setup

```bash
cp .env.example .env
# Fill in your API keys in .env
uvicorn main:app --reload
```
